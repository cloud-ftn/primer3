name: Deployment-v3
on:
  push:
    branches:
      - main
      - dev
env: 
  MONGODB_NAME: db
jobs:
  test:
    env:
      MONGODB_CLUSTER_ADDRESS: cluster0.725n4gp.mongodb.net
      MONGODB_USERNAME: oblakftn_db_user
      MONGODB_PASSWORD: aKK1ei2hisLBn6A4
      MONGODB_DB_NAME: db
      PORT: 8080
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: |
          echo ${{env.MONGODB_USERNAME}}
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: | 
          echo ${{env.MONGODB_USERNAME}}
